# Runtime artifacts — never commit
qq_remote_bridge.log
qq_remote_bridge_watchdog.log
qq_remote_bridge.pid
qq_remote_bridge_watchdog.log

# Local config (contains real secrets) — must never be committed
qq_bridge_config.json

# OS junk
Thumbs.db
Desktop.ini
